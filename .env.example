# Copiá este archivo a ".env.local" en la raíz del proyecto y completá los valores.
# Guía paso a paso: Documentacion/primer-arranque-supabase.md

# Supabase → Project Settings → API
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Solo servidor (crear capitanes). Nunca exponer en el cliente ni commitear .env.local
SUPABASE_SERVICE_ROLE_KEY=
